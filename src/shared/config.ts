// Backend + frontend URLs. The build embeds these via `define` (see vite.config.ts).
// For dev vs prod, swap by editing here or wiring an env file later.

export const API_BASE_URL = "https://scout-backend.ngrok.app";
export const WEB_APP_BASE_URL =
  "https://patronly-pyrographic-amelie.ngrok-free.dev";

export const EXTENSION_LINK_PATH = "/extension-link";
